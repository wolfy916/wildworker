package com.a304.wildworker.dto.response.common;

public enum MiniGameType implements SubType {
    MATCHING,       //매칭
    CANCEL,         //취소(도망)
    START,          //시작
    RESULT,         //결과
}
